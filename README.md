# 招標網資料自動抓取器 (Procurement Data Scraper)

這是一個能讓您輸入「政府電子採購網」等標案網址後，自動將標案的相關資訊（如機關名稱、得標廠商、決標金額等）抓取並直接寫入您個人 Google 試算表的小型網頁應用程式。

## 系統架構

1. **前端網頁 (Frontend)**：
   - 採用 **Glassmorphism (玻璃擬物化)** 設計風格的現代化介面，支援**單筆或批次貼上**多組標案網址同步抓取。
   - **歷史紀錄總表**：包含即時連動、快取秒開、多種日期排序功能，並將網址與標案名稱合併以優化排版。
   - **互動式狀態管理**：可直接在網頁核取「結束」狀態，自動雙重確認並同步至雲端試算表。
   - **高對比視覺設計**：針對深色模式進行調優，確保超連結在高解析度螢幕與手機端皆具備極佳的可讀性。
   - 由 `index.html`, `style.css`, `script.js` 組成，可部署於任何靜態網頁伺服器（推薦 GitHub Pages 或是直接開啟本地網頁）。

2. **後端代理服務 (Backend / GAS proxy)**：
   - 利用 **Google Apps Script (GAS)** 避開了瀏覽器惱人的跨網域限制 (CORS 問題)。
   - **智慧欄位對應**：會自動去讀取您的試算表「第一列(表頭)」目前有哪些名稱，並智慧填寫進去，因此更改您的試算表欄位順序也**完全不會壞掉**。支援相容多種常見欄位寫法（如「招標網址」、「網址」等）。
   - 由 `GAS_Script.js` 裡面的程式碼驅動，當作 API 給前端呼叫。

---

## 🛠 更新日誌 (Update Log)

### [2026-04-15]
- **UI 調整**：大幅精簡資料列表欄位。
    - **隱藏座標資訊**：隱藏了「機關/廠商地址緯度與經度」等 4 個技術欄位，顯著減少表格橫向捲動的負擔。
    - **地圖無縫運作**：核心地圖渲染邏輯維持不變，標記點依然能顯示在正確位置。
    - **欄位顯示優化**：移除了重複的網址連結，並根據回饋保留「抓取時間」欄位，在簡潔與實用間取得平衡。

### [2026-04-14]
- **功能改進**：強化「結束」狀態篩選邏輯。
    - **地圖同步隱藏**：現在標記為「結束」的標案也會同步地從地圖模式中移除，確保資料列表與地圖顯示內容完全一致。
    - **效能優化**：彙整篩選邏輯至核心處理流程，提升資料渲染效率。

### [2026-04-13]
- **功能大改版**：標案地圖工具 2.0 全面進化。
- **功能改進**：實作「座標聚合 (Smart Clustering)」功能。同座標的多筆標案會自動合併為單一標記並顯示數量，解決點位重疊無法選取的問題。
- **功能改進**：強化地圖彈出視窗 (Popup)。
    - **身分動態辨識**：自動顯示機關/得標廠商名稱。
    - **一鍵導航**：地址轉為 Google Maps 連結，點擊即可進行路徑規劃。
    - **資訊彙整**：同時呈現公告日與到期日，並強化標案名稱視覺效果。
- **UI 調整**：新增「標記類型篩選器」，支援自由切換顯示機關、廠商或全部地址。
- **Bug 修復**：修復初始化順序導致的 JS 報錯，並導引使用者更新 API 網址以正確取得經緯度座標。

這份專案是在經過多次優化後成型的。以下是主要的演進紀錄：


### [2026-04-07]
- **功能改進**：新增「重複網址預檢」防呆機制。在正式抓取前，系統會自動比對歷史紀錄與輸入內容，若發現完全重複的網址會自動過濾並顯示提示，或彈出確認視窗引導使用者，有效防止重複消耗 API 配額。
- **功能改進**：新增「抓取中斷 (停止)」功能。原本的「一鍵抓取」按鈕在任務執行期間會動態轉變為醒目的紅色「停止」按鈕，使用者可隨時點擊以安全中斷當前的批量抓取任務。
- **UI 調整**：優化執行狀態視覺動效。為「停止」按鈕設計了專屬的紅色漸層 (#ef4444) 與懸停發光效果，並配合狀態文字同步更新（如「正在中斷...」），讓自動化過程更具可控感。

### [2026-04-05特別更新]
- **功能改進**：Google 試算表同步優化。現在存入試算表時，會自動將「標案名稱」透過 `=HYPERLINK` 公式轉為可點擊連結，讓試算表操作更便利。
- **功能改進**：實作「抓取間隔延遲」機制。每筆網址抓取後自動等待 10 秒（包含視覺倒數顯示），有效防禦採購網的連線頻繁驗證偵測。
- **UI 調整**：優化表格空間。移除獨立的「網址」欄位，改將超連結內嵌於「標案名稱」中，精簡表格寬度。
- **UI 調整**：強化超連結色彩對比。針對深色背景選用亮天藍色 (#60a5fa) 並加入 Hover 發光動效，大幅提升閱讀體驗。


### [2026-04-04]
- **功能改進**：新增雲端代理換 IP 功能 (Proxy API)，最初整合 ScraperAPI 並隨後遷移至 WebScraping.ai 服務。這讓後端 GAS 抓取資料時能自動更換 IP 並繞過採購網的驗證碼機制，支援關閉 JS 編解碼以節省額度，徹底解決公司網路 IP 被鎖定的問題。
- **UI 調整**：優化標案抓取表單的版面配置。

### [2026-03-14]
- **功能改進**：新增「標案類別」篩選選單。位置安排於機關篩選左側，提供「補強、道路、修復、防水、其他」等選項，系統會根據「標案名稱」內容自動進行智慧關鍵字比對與過濾，並可與地區篩選功能多重連動。
- **UI 調整**：新增專屬應用程式圖示 (Favicon)。解決強制重新整理 (CTRL+F5) 時出現的 404 報錯問題，並提升網頁整體的專業視覺形象。

### [2026-03-13]
- **功能改進**：新增「採購網驗證碼自動偵測」機制。後端 GAS 會自動辨識是否進入撲克牌驗證碼頁面，若偵測到則由前端彈窗引導使用者手動驗證，避免解析失敗。
- **UI 調整**：新增「過期標案自動警示」功能。系統會自動比對當前日期與「結束日」，凡是已過期的標案，該列文字將自動顯示為鮮艷紅色，方便一眼辨識。

### [2026-03-12]
- **功能改進**：新增「軟性重複資料示警」防呆機制。前端在送出抓取請求前會自動比對歷史紀錄，若發現網址重複將彈出視窗詢問，由使用者決定是否繼續存入。
- **功能改進**：新增「接觸」人員雙向連動功能。網頁歷史紀錄表中的接觸狀態改以選單呈現（凱、娟、喬），並支援在網頁直接切換即自動同步更新至雲端試算表紀錄。
- **功能改進**：GAS 腳本自動化功能升級。現在抓取新資料存入試算表時，若存在「接觸」欄位，系統將偵測並自動套用下拉式選單資料驗證規則。
- **UI 調整**：優化「接觸」下拉選單寬度與間距，修正原本文字與箭頭重疊的問題，並修正選項文字錯誤。
- **UI 調整**：擴充篩選功能。機關與廠商地址過濾器新增「新竹縣市」分類，並同步更新「其他」地區的自動排除邏輯。

### [2026-03-09]
- **UI 調整**：在歷史紀錄總表右上角新增「機關」與「廠商」的下拉式篩選選單。
- **功能改進**：實作前端即時地址篩選邏輯，選單支援「全部」、「台北市」、「新北市」、「桃園市」與「其他」。其中「台北市」選項能智慧相容「台北」與「臺北」兩種寫法並自動排除「新北市」，達到精準篩選，且篩選結果與排序功能可無縫連動。

### [2026-03-08]
- **功能改進**：新增智慧日期拆分功能，當試算表標頭設定為「開始日」與「結束日」時，GAS 會自動抓取「履約起迄日期」並切分為兩欄（且自動濾除「預估」等文字），同時前端的下拉選單也一併更新為這兩個新欄位的排序。
- **Bug 修復**：針對爬蟲抓取到的日期年份，實作正規表示式去除開頭多餘的 `0`，例如將 `0115/03/17` 修正為 `115/03/17`。

### [2026-03-04]
- **UI 調整**：前端網頁介面變更為全螢幕，移除毛玻璃與背景的浮動動畫特效，並解除歷史紀錄清單的高度限制，讓資料能更完整呈現。
- **UI 調整**：將原本上下佔空間的「輸入網址」跟「一鍵抓取並寫入」按鈕區塊，使用 CSS Flexbox 改為左右並排，成功節省網頁的垂直高度。
- **功能改進**：實作了可以讓 AI 代理人自動記錄更新日誌到 README.md 的功能，減少手動維護的工作量。
- **功能改進**：精簡並改進了對話與自動輔助的指令流程，讓後續工作更有效率。

### [2026-03-03]
- **功能改進**：精簡設定單純化與預設優化，依照您的指定，將長長一串排序列表精簡為明確的「5種標準配置」(依照 存入時間/公告日/履約起迄日期 最新和最舊、以及機關和廠商地址的前3碼順序)，確保預設資料會最新顯示在前方。
- **Bug 修復**：修復預設圖表空白問題，透過嚴格修正腳本迴圈括號(`script.js` line 226)的包裹作用域，讓選取「存入時間(預設)」時，迴圈能脫險正常載入全部列表資訊！

### [2026-03-02]
- **功能改進**：自動公式注入 (XLOOKUP)，抓取新標案存入試算表時，GAS 將自動計算當前列數，並在「業務」欄位注入 `=XLOOKUP()` 公式。
- **功能改進**：「結束」狀態互動式打勾，前端在抓取到資料且該資料的「結束」欄位為未核取 (`FALSE`) 時，會直接在畫面上繪製出真正的 HTML Checkbox。使用者打勾時會跳出 double-click 確認視窗防呆。確認後呼叫新增的一支 **Action API (`update_status`)**，遠端更新試算表把那一格打勾。前端網頁再次重整後會自動濾除（隱藏）所有被標記為 `TRUE` （已結束）的資料。

### [2026-03-01]
- **功能改進**：歷史資料連線 (GET 請求)，新增讀取 API，讓試算表上的資料可以直接匯出顯示於前端首頁。
- **功能改進**：快取秒開技術 (LocalStorage)，打開網頁或重新整理時，優先載入上一次瀏覽器記錄的舊資料快取，背景再默默更新最新資料，達成「零等待」的使用體驗。
- **UI 調整**：全螢幕延展彈性佈局，移除不必要的中間預覽表格，並將畫面可用寬度拉滿（依賴解析度彈性最高達 1600px寬），改善資料擠成一團的問題。
- **功能改進**：前端自訂排序功能，新增右側下拉選單，自動擷取「115/01/22」這樣的民國年份字串，轉換為程式可讀時間序列，支援公告日、履約日期的最新或最舊排序。
- **UI 調整**：響應式手機版設計 (RWD)，增加 `@media` 查詢，螢幕過小時表格將轉為可橫滑捲動的模式，版面不再被生硬壓縮。

### [2026-02-28]
- **初步架構**：建立基礎架構，完成前後端分離設計，使用 GAS 作為代理伺服器抓取原始 HTML，以此繞過前端網頁的 CORS 限制。
- **功能改進**：動態表頭匹配機制，GAS 腳本自動讀取 Google Sheet 第一列的文字，不綁死固定欄位，試算表可以隨意移動欄位順序。
- **功能改進**：爬蟲邏輯強化，針對特定的數字欄位（如「決標金額」）加入 Regular Expression 處理，剔除中文字只保留「15,980,000」這樣的純數字與逗號格式。

---

## 部署教學（當 GAS 程式碼有更新時）

只要您的 `GAS_Script.js` 程式碼有任何變更或升級，您都**必須**回到您的 Google 試算表中進行更新：
1. 點擊試算表上方的「擴充功能」 > 「Apps Script」。
2. 將 `GAS_Script.js` 的所有文字複製，貼上並覆蓋掉裡面的程式碼。
3. 點擊右上角「部署」 > 「管理部署作業」。
4. 點擊彈出視窗右上方的「編輯 (鉛筆圖示)」。
5. **在版本的地方點開下拉選單，選擇「建立新版本」。**
6. 點擊「部署」。

> *沒有建立新版本的話，Google 伺服器會一直執行您的舊時代程式碼！這點非常重要！*
